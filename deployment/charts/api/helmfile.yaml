releases:
  - name: laos-client-release
    namespace: '{{ .Values.namespace }}'
    createNamespace: false
    chart: .
    values:
      - imageTag: {{ .Values.imageTag }}
      - host: {{ .Values.host }}
